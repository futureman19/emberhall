# Browser placement slice

Nine buildable kinds: each missing-kind disposable world found a site on unchanged runtime terrain with valid siteError and astar result. Real local8093 renderer showed one visible nonempty instanced basic-material ghost at opacity0.34 and valid gold c9a36a. All9 ghost structural checks passed. All9 real store useTile placement dispatches created the requested building, deducted correct gold and cleared build mode. No page errors.

Raw art/lane3/browser-placement/results.json enumerates all9; nine screenshots retained but not visually reviewed. These are structural rendering checks, not full voxel-transform/picking/visual parity. Player was repositioned at court; astar existence is not walking completion. Action invoked through store, not a pointer click. Construction animation, desktop/mobile visual review and failure-fallback coverage remain open. This batch used desktop viewport and controller existing local dev renderer, not isolated lane3 server or deployed validation. No runtime/save files modified or deployment.

Next consolidate source/predicate/browser evidence without claiming remaining visual/animation acceptance; implement pointer check and animation assertions as separate stages.
