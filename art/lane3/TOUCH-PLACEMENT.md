# Mobile touch placement and effect dispatch

Initial nine-kind mobile390x844 batch: eight commits/effect dispatches passed, farm failed. Preserved raw touch-placement/results.json passed=false. Farm projectedx=-172.52 was outside viewport, not a valid touch-target test.

Explicit different fixture followup positions player at valid farm site, waits for camera, asserts onscreen target before actual tap. Farm commit/effect passed; touch-farm-centered/results.json retained separately. Combined unique kind count9; initial batch not retroactively green. Eight use court-position fixture, farm uses site-centered fixture. Neither is build-menu selection or actual player traversal to site.

Each successful tap verified gold/count/coordinates/build-mode exit and matching construction effect buildingId/kind. constructionPose active/end contract evaluated, not animated renderer visibility. Animation playback, cleanup and visual review remain untested; no inference from emitted event. Desktop GPU touch emulation, not physical phone. No page errors, no runtime or deploy changes.
