# Actual mouse placement

Nine kinds passed on existing controller local8093 dev renderer. Missing-kind disposable fixture selected build mode, then actual Playwright mouse click at groundY/camera-projected site placed each building. All9 gold deductions, counts, exact coordinates and build-mode exit asserted. All9 valid ghost structural checks passed. No page errors. Raw pointer-placement/results.json includes every pointer coordinate.

This closes desktop pointer dispatch for these fixtures, not build-menu selection, touch/mobile, physical device or construction-animation visibility. No code/save/terrain/runtime edits or deployment. Animation remains separate; do not infer it from successful construction.
