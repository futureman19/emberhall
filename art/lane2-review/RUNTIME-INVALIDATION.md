# Runtime invalidation parity

Final varied local-browser batch12cases: desktop/mobile ×two origins ×atlas changes/output-buffer replacement/unchanged. All exact geometry/instance-buffer and rendered-pixel comparisons matched forced original full loop; no page errors. Every atlas case toggles ridgewatch coordinates. Every buffer case replaces all four near-ground output arrays with zeroed fresh arrays and verifies against full rebuild. Unchanged compares skipped vertex writes to full path. Normal/bounds finalization retained.

First batch also12passes but repeated identical atlas coordinates after first mutation; not twelve independent mutation proofs. Retained civic/ground-planner-invalidation.json; stronger evidence civic/ground-planner-invalidation-varied.json and corresponding script. Disposable browser only; local atlas module reset with browser teardown. Lint passed. Runtime source unchanged from60bc24e. No deployment/performance claim. Next production build ABBA before rollout.
