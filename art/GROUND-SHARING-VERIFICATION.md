# Ground biome sharing — local candidate

Ground colorAt/coverAt now accept the same biomeWeights result computed once per vertex in near and far ground loops. No retained cache, invalidation key, sampling density or refresh-timing changes. This removes one duplicate pure biome evaluation per valid ground vertex. Prepared-footprint experiment remains parked.

Red source contract then green. Four desktop/mobile same-state cases compare all mesh geometry attributes, instance count/matrix/color and PNG against recomputed helper weights; exact matches.206 script tests +562 game tests, lint/typecheck/build passed.82 local production settlement checks and2 controlled real-time completion outcomes passed. Single unprofiled completion maxima hatchet199.9ms,hoe166.7ms; this is not a controlled performance improvement claim.

Local-only checkpoint, not deployed: prior lessons require controlled same-build performance before publishing another optimization. Current review preview remains qntrco7ac. Next compare shared vs original double sampling with equivalent production-build instrumentation and unchanged action fixtures; retain stalls and cold/movement cases. No global performance acceptance.
