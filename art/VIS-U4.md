# VIS-U4-r1 — campfire and settlement sign polish
Status: implementation frozen for independent QA, NOT accepted or deployed.
Base: VIS-U3-r1 plus independently checked U3 smoke hygiene patch. U1/U2/U3 now have scoped independent passes.

Changes: two shared authored campfire geometry parts in existing six stone/two log placements; original materials, transforms, flame/light, gameplay and fallback preserved. Forge existing seven-triangle anvil pictogram receives brighter warm gold (.95,.72,.30) and restrained .08 emission strength. No point light added. Geometry, index/normal bytes and other forge materials remain exact. Bank GLB restored byte-exact after Blender regeneration; all other approved buildings remain unchanged. No new doors, signs or camera rules. Named editable settlement .blend and generator include sign material.

Blender regeneration changed binary geometry bytes even with unchanged JSON. Final forge export intentionally transplants only newly authored material JSON onto the prior approved GLB binary chunk; preserved original is evidence VIS-U4-sign/forge.glb. To reproduce exact runtime output after regeneration: keep the regenerated sign material record, retain all original JSON except that material, and retain the entire prior binary chunk. Run scripts/measure-settlement.mjs after generation/material-only output; it refreshes measured manifest required by tests. Original generator backup in evidence; do not regenerate bank unnecessarily.

Fresh gates: Blender source reopen passed; GLTFLoader measurement/front-door rays/roof rays pass; npm run typecheck, full lint, npm test (990/990), art audit (errors empty), npm run build all exit0. Final script-only comments/original-sign control added after build; full lint/audit rerun green. Served four HTML-linked local JS/CSS plus forge/campfire GLBs match built bytes.

Evidence: final-fire-dev and final-fire-dev-reject (2 cases each, candidate eight attachments, rejection zero); final-fire-built (2 screenshot cases); sign-built and sign-original (six settlement census views each). Census viewpoints are fixture-positioned, not entrance action acceptance. Normal mobile image comparison supports only a subtle nonintrusive visibility improvement, NOT fine icon recognition. Foreground building occlusion remains unchanged.

Retained failed runs: u4-final-test.log missed required post-generation measurement fields (undefined bytes); measurement refreshed, no tests weakened, full r2 green. final-fire-rejected mistakenly ran dev-introspection fallback against built server and failed on /src import; correct dev fallback run is final-fire-dev-reject. External Grok extension 403 excluded from local asset byte comparison; browser runs had no unexpected errors. No runtime patch for those harness/invocation failures.

Stopping point: campfire + modest existing sign polish is the bounded U4 candidate. Bank/tavern unchanged. No speculative interior/moongate/roof-layout changes were made; further changes need actual findings. Independent native cooking/construction/expiry/picking/save/reload, building entrance/cutaway/service regression and asset rejection/late-load checks remain requested, not claimed. Physical phone, dense natural long sessions, exclusive-GPU performance and combined U1–U6 release QA remain separate. Preserve original log arrangement; no claim of millimetre contact.
