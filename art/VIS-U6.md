# VIS-U6-r1 — loot-panel finishing and integrated QA candidate
Base: independently scoped accepted VIS-U5-r1 (includes accepted U1–U4 and U4 live-render patch). U6 implementation complete for bounded UI scope, independent U6/combined acceptance pending. No deployment authorized.

Runtime changes only context-menu.tsx and hud.tsx: accessible44x44 Close loot clears openPileId only; bounded flex loot panel with scrollable list and fixed header/Take all; loot notifications displayed in bounded panel flow; global Toast deferred while an existing pile is open, existing house suppression retained. No inventory/game/store/schema changes. Art ledger refreshed for source line shifts. Four source-contract regressions added in scripts/loot-dismiss.test.mjs.

Fresh full998/998tests,typecheck/full lint/audit/build0. Dev and fresh-built desktop1440x960/portrait390x844/landscape844x390 pass: close44px reachable, Take all reachable, notification rectangles disjoint from controls, native wheel/touch list scrolling, dismiss deep-equals piles+pack+gold, reopen transfers exact15items+7gold. Four HTML-linked local JS/CSS byte verified on8157. Screenshot reviewed: no blocked Close/Take all; clipped rows are scrolled list boundaries. Fixture opens panel via store and supplies items, not proof of native world-pouch opening. Notification text is synthetic UI fixture, NOT a claimed game capacity restriction.

Retained failures: first fixture used invalid cloth item, corrected to catalog hide then legitimate missing-close red reproduced; synthetic CDP scroll gesture didn't move mobile list, actual touchStart/move/end passed. Long notification overlapped Close in short landscape before in-panel fix; current rectangles green. First final full suite expected original house suppression literal; retained original guard plus separate pile guard instead of weakening existing test, full rerun green.

Stopping point: no further speculative visual edits while final QA runs. Combined U1–U6 acceptance still needed, as do physical phone, exclusive GPU/frame timing, long natural sessions and prior explicitly untested environment states. Accepted earlier slices are not blanket release clearance. No committed/pushed/deployed change or worktree cleanup. QA uses immutable packet in own copy; no auto-merge diagnostics.
