# Same-build hoe cache control

Current deployed routes-ConYKe08.js in every arm; browser-only bypass returns original biome sampler result before cache lookup. Bypass/cache/cache/bypass, one hoe action each after8500ms settle; no CPU profiler. Direct route token asserted unique. All4 outcomes passed, each recorded exactly1 terrain rebuild. Lint passed. Summary hoe-cache-control-summary.json and raw actions-hoe-* results retained.

Worst frame(ms): bypass216.8/266.6; cache300.0/233.3. Ranges overlap and do not give a consistent direction across paired runs. This bounded test does not establish that cache is the cause or that it is regression-free. Prior deployed parent comparison's worse hoe result remains open. Control isolates hoe alone; previous comparison ran hatchet first, so do not pool their frame distributions as equivalent fixtures.

The large completion hitch survives both cache paths. Stop repeating whole-action ABBA timings without better attribution. Next meaningful step is event-level separation of terrain rebuild CPU, React work, and GPU submission/upload around the single revision; unprofiled timings alone cannot identify the cause. No runtime changes, deployment, threshold changes, or production promotion.
