# Active-count multi-location parity — intermittent failure, blocked

Six cases: desktop1440x960/mobile390x844 at256,290;220,260;280,320,8500ms settle each. Direct fixture relocation tests streaming-window recomputation, NOT continuous walking. At each location compare current active counts to restored old full-capacity counts only for buffers with original hidden-tail sentinel matrices; then restore. Exact synchronous canvas PNG comparison plus first five intersection object IDs/instance IDs/distances for nine NDC rays. This is sampled raycast parity, not every resource's real UI action.

First active-count-streaming-parity.json:6/6 pass. Active slots changed and grew across locations (solid trees921->919, faded0->1->3, rocks107->96->108). Repeat active-count-streaming-final.json FAILS at third desktop case280,320: pixelsEqual=false, restoredEqual=true, picksEqual=true. Earlier two cases pass. Do not discard this failure. Image capture was added afterward; active-count-streaming-capture.json passes all6 and therefore produces no mismatch images. Root cause and pixel-difference extent remain unknown. Lint passes.

Acceptance blocked: cannot claim repeatable visual parity or deploy77daea2 from these results. Local optimization remains available for diagnosis; external preview unchanged. Next must capture/localize intermittent difference, including shadow effects and whether broad diagnostic expansion touches non-terrain batches, before additional performance or deployment claims. First run retains copied metadata from original single-frame probe; actual six results are authoritative. Later harness metadata corrected. Raw files preserved.
