# VIS-U5-r1 — bounded environment verification and sky-haze portability
Implementation ready for independent QA, not accepted/deployed.
Base: independently accepted VIS-U4-r1 + live-render patch. Runtime U5 change is ONLY defined GLSL downward sky-haze ramp (1-smoothstep(-.5,-.05,y)) replacing reversed-edge smoothstep. Existing color/endpoints retained. No new geometry, material counts, terrain, roads, water, camera, streaming, action timing, RNG, save or gameplay changes. Art ledger line positions refreshed. Two shader regression tests added.

Retain current shoreline: built natural terrain at359,394 seed2469134 has readable water/sand/grass transitions. Captures are fixture-positioned, NOT native approach/collision acceptance. Full current suite994/994, typecheck/full lint/audit/build0. Four HTML-linked built JS/CSS bytes verified. Six built shore scenes (desktop/mobile x day/night-clock/rain) exercise native Work open/Close, without /src introspection. Eighteen clean-view dev cases cover current shader, old expression control and true-night diagnostic; all no page/console errors. Initial Guide-covered runs retained but superseded for visual judgments.

DEV_DAYLIGHT=true remains intentionally unchanged. Hour0 current scene is NOT true night; browser-only diagnostic false override is darker and less readable. Do not silently ship that override or call diagnostic screenshots current output. Original expression control intercepts Vite-transformed source, not gameplay. GPU program errors checked; no performance claim. Main portability red/green tests inspect shader ramps and numerical bounds; initial drafted count five corrected to actual four and red proof repeated. Early full run required canonical ledger refresh; no tests weakened. Failed logs retained.

U5 stopping point is limited correction plus environment review, not a promised water/lighting redesign. No evidence justified speculative additional props/recoloring. Independent biome/clock transitions, natural travel, shader compatibility and dark/rain readability still need QA. Physical phone, long dense sessions, exclusive GPU and combined U1–U6 release remain separate. Next implementation phase U6 UI/cohesion, including PileGump dismiss.
