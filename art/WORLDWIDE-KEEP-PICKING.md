# Retained keep — visible-surface pointer fix verified; stair visibility partial

## Delivered locally
`building-meshes.tsx` now handles primary and secondary pointers inside the keep during play, after the existing build-hold branch. It rounds the actual rendered hit's X/Z and delegates to the existing `leftAt` / `hitAt` paths, instead of falling through to displaced ground below. Other building, exterior, phase and middle-button handling remains unchanged. This is not a new navigation, collision or touch-gesture system.

The fresh original-pointer control reproduces `(181,320,7.11)` commanding `(175,314)` on desktop and touch. Candidate, actual surrounding-art rejection and rebuilt output command `(181,320)` and then reach it through normal simulation. No floor teleports or injected positive walk/menu commands.

## Evidence and boundaries
- Source-coupled handler regression: original **7 pass / 2 fail**, current **9 pass**. Architecture + keep-story + city focused run **23 pass**. The AST extracts the real handler and stubs dependencies; it is not a duplicate implementation.
- Final full gates: **962 tests** (250 + 712), typecheck, full lint, build, auth invariant and diff checks pass. Logs: `.hermes/keep-picking-tests-final2.log`, `keep-picking-{typecheck,lint,build,auth}-final.log`. First full run failed the stale AST inventory; `audit-art-coverage.mjs --refresh` repaired routing without resetting existing evidence/dispositions/states across **3,148** ledger assets. The initial failure log is retained.
- Fresh bounded browser checks: candidate **214**, surrounding-art rejection **216**, built **190** = **620**. Separate original-pointer control **208** verifies historical behavior, including its expected elevated-target miss. Combined **828** assertions, **8** mode/device cases and **152** native-input legs. These counts are this slice only, not the earlier ceiling slice's coincidentally equal total.
- Each case uses desktop1440x960 or touch-enabled Chromium390x844 on canonical seed2469134 save-loaded terrain, with other actors removed only for input isolation. **19 legs** include south entry, ground target, stairs upward to1/2/3, lateral landings, the exact181320 regression point, descent3→2→1, then native mini-map exit and exterior restoration. The original control uses ground-projected waypoints; corrected modes use exposed keep surfaces while inside. This is not complete visible-stair descent acceptance.
- Native desktop secondary clicks on clear floor points open the correct tile menu without changing walking intent/path; Cancel is clicked normally. Mobile acceptance uses actual `touchscreen.tap`, not mouse clicks at a narrow viewport. No long-press or all-modes exactly-once claim: the existing native `PlacePointer` remains independently active in building/tilling modes.
- Candidate/failure development geometry preserves exact original voxel multisets and exterior restoration. All accepted modes preserve canonical terrain and building records. Physical route X/Z/story/facing/path tracks agree. Original-control pre-repeat intent/path deliberately differs after its wrong-target probe; built vs dev differs only in early weather-toast timing. Exact differences are enumerated in `keep-picking-audit.json`; whole transient-state equality is not claimed.
- Of **410** checkpointed source/package/art files, only `building-meshes.tsx` and its appended `architecture-kit.test.ts` changed. All **114 GLB/Blend** files, simulation, pointer dispatch helpers, terrain, camera and save source remain unchanged. Ledger refresh and new QA/docs are additional non-runtime changes. Independent review found no scoped must-fix and independently repeated the red/green handler tests.
- The keep has no GLB. Rejection mode actually rejects **168** surrounding/character-art requests; this is not a keep-model fallback test.
- Long-lived preview served two removed old JS chunks after rebuild; only that tracked process was restarted. All **4** new HTML-linked JS/CSS assets match `.vercel/output/static` bytes before built acceptance. Built desktop/mobile screenshots show the original keep and exposed player; still images do not prove input or physical contact.

Artifacts: `art/verification/worldwide/keep-picking-{candidate-bounded-v1,original-control,failure-final,built-final}/`; aggregate `keep-picking-audit.json`; freshness `keep-picking-built-freshness.json`; checkpoint `keep-picking-source-before.json`; ledger preservation `keep-picking-ledger-preservation.json`.

## Still open — not waived by the bounded pass
1. **Lower-stair visibility.** A tower conceals the centre of the stair-base tile. The ascent harness uses an actually exposed edge of that same tile; no art/terrain/camera changes. From story1, the tower and deck obscure intended lower stair targets. Failed attempts and first-hit rays remain in `keep-picking-candidate-v1/v2/v3/v4` and `keep-picking-descent-ray`. The final harness exits with the real mini-map; that does **not** prove direct lower-stair tapping or an ordinary full descending route. The pointer now targets the actual foreground surface; canonical walking may redirect an unwalkable wall target to a nearby tile. No click-through-occluder behavior is promised.
2. **Floor/stair seating.** The existing deck extends roughly .51 above the player root. This patch does not change model/root placement or physical contact.
3. Natural actors, every interaction/mode/angle, full animation, construction/mesh collision, physical phones and performance remain unaccepted.

## Closure self-audit
- **Incomplete:** Lower-stair visibility and floor seating above keep overall acceptance partial. Next bounded work is the obscured lower-stair input seam, then seating.
- **Untested:** No all-modes native single-delivery proof, physical phone, full natural world, performance or publication. Built scene-private geometry inspection is unavailable; byte freshness and actual input/state are used instead.
- **Pattern check:** Existing terrain-style immediate button0 and shared pointer dispatch are preserved; no new dependency or gesture implementation. Nine actual-handler regressions include other building/build/phase/middle-button cases.
- **Hidden concerns:** Initial stair samples were behind the tower, not visible-floor picking defects. Failed evidence was retained and scope narrowed explicitly; the final map exit is not counted as a repaired visible descent. Initial stale-ledger/build-preview failures and weather-toast differences are disclosed above.
- **Cleanup:** Earlier `worldwide-keep.mjs` is a historical ground-projected ceiling harness, not the current pointer contract; use `worldwide-keep-picking.mjs` for this bounded current slice. Raw diagnostics are intentionally retained. No orphan production features/dependencies introduced; local dev/preview processes remain tracked.

**Visible keep-surface targeting is locally verified. Complete keep usability remains partial. No commit, push, public preview or production publication.**
