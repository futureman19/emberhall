# Production-built hall / hoe diagnostic

Eight sequential mobile390x844 runs: hall and idle hoe each fallback/current/current/fallback,8500ms settle and300 RAF intervals. All completed without page errors; requested GLBs loaded in current arms and route aborts recorded in fallback arms. Lint passed. Raw production-fixture-perf.json retained.

Hall p95 fallback16.8/16.8ms; current16.7/16.7ms. Hoe fallback16.7/16.8ms; current16.7/16.7ms. These runs do not reproduce the older49.9ms failures. No speedup claim: tiny differences are timing noise.

Limitations: same current production runtime in both arms, not historical parent. Hall disables Phase1 interiors/signs; hoe disables approved tools GLB. Frame timings only; successful loading is not scene-graph or visual proof. Hoe is the original idle tool fixture, not active farming. Defaults explicitly shadows on/reduction0; fixed noon/clear weather. Mobile viewport on desktop GPU, not physical-phone hardware. No global acceptance clearance or retroactive waiver of original failures.

No runtime/art changes or deployment. Next acceptance should reconcile original gates with equivalent production-built parent/candidate comparisons and validate active actions separately, not pursue speculative scenery reductions.
