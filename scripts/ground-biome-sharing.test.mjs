import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('near and far ground share one exact biome sample per vertex',()=>{const s=fs.readFileSync('src/components/game/terrain.tsx','utf8');assert.equal(s.split('const weights = biomeWeights(wx, wz);').length-1,2);assert.equal(s.split('colorAt(w, wx, wz, pal, weights);').length-1,2);assert.equal(s.split('coverAt(w, wx, wz, karr, i, weights);').length-1,2);assert.equal(s.split('const w = weights;').length-1,2);});
