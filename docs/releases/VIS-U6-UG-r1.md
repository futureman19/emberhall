# Production release VIS-U6-UG-r1

Source checkpoint for deployment dpl_9ZdVdxFrDtVvocPmT392XLULN1Gg at https://emberhall-vale.vercel.app. Approved U1-U6 plus independently passed underground correction.

1004 tests and full gates passed for this exact source. User subsequently reported phone play appeared fine: user-verified smoke, not hardware certification. Performance, long wilderness and visual night-sight exclusions remain explicit in companion JSON.

Configuration includes only the sanitized auth-off flag; no live private environment file copied. npm ci then npm test/typecheck/lint/build use the normal project wrappers. This source tag is a rollback/rebuild reference, not a promise of bit-identical build hashes across environments. Use the recorded approved deployment for exact-artifact rollback.
